[pytest]

addopts = -v -ra --tb=short
testpaths = tests
markers = 
    slow: marks tests as slow (deselect with '-m "not slow"')
    db: tests requiring a ChromaDB database (deselect with '-m "not db"')
    integration: test the full pipeline flow

